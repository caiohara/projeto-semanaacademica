# Interface

- Nos testes, a API é sempre a falsa (MSW); nenhum teste chama a API real.
- Interaja com a tela como usuário: por texto, papel e rótulo, nunca por estado interno do componente.
- Rode os testes com: npx vitest run
